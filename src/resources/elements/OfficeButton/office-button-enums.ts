export enum ButtonEnabled
{
    enabled,
    disabled
}
export enum ButtonChecked
{
    unchecked,
    checked
}
export enum ButtonType {
    default = 1, 
    primary, 
    command, 
    compound, 
    hero,
    icon
}